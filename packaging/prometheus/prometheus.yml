global:
  scrape_interval: 5s

scrape_configs:
  - job_name: whispera
    static_configs:
      # From inside Docker, use host.docker.internal to reach host services on Windows
      - targets: ['host.docker.internal:9101', 'host.docker.internal:9102']

