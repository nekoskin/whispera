# Whispera ML Engine — Server (Linux VPS, CPU only)
#
# install.sh автоматически выбирает стек по ресурсам сервера:
#
#   minimal  (1 ядро / < 2 GB RAM)  → onnxruntime + sklearn   ~130 MB
#   standard (2-7 ядер / 2-8 GB)    → onnxruntime + sklearn   ~130 MB
#   full     (8+ ядер / 8+ GB RAM)  → tensorflow-cpu          ~600 MB
#
# Ручная установка:
#   pip install -r ml_engine/requirements-server.txt         (onnxruntime)
#   pip install tensorflow-cpu                               (только для full)
#
onnxruntime>=1.16.0
skl2onnx>=1.16.0
numpy>=1.24.0
scikit-learn>=1.6.1,<1.7.0
scipy>=1.10.0
joblib>=1.3.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6
