# Whispera ML Engine — Client (Windows)
#
# Установите ОДИН пакет onnxruntime в зависимости от GPU:
#
#   NVIDIA GPU (CUDA):
#     pip install onnxruntime-gpu
#
#   AMD / Intel GPU (DirectX 12, Windows):
#     pip install onnxruntime-directml          ← текущий вариант
#
#   Только CPU (Intel / AMD, любая ОС):
#     pip install onnxruntime
#
# Код автоматически определяет доступный провайдер:
#   CUDAExecutionProvider → DmlExecutionProvider → CPUExecutionProvider
#
onnxruntime-directml
numpy>=1.24.0
scikit-learn>=1.6.1,<1.7.0
scipy>=1.10.0
joblib>=1.3.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6
